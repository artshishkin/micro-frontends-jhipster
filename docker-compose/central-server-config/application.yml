#common configuration shared between all applications
configserver:
  name: Docker JHipster Registry
  status: Connected to Consul running in Docker

jhipster:
  security:
    oauth2:
      audience: https://dev-nfiux7r7sr1uxj1g.us.auth0.com/api/v2/

spring:
  security:
    oauth2:
      client:
        provider:
          oidc:
            issuer-uri: https://dev-nfiux7r7sr1uxj1g.us.auth0.com/ # make sure to include the trailing slash!
        registration:
          oidc:
            client-id: a1x...zE1
            client-secret: oE4_L...qIm